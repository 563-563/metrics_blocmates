# Template — copy to .env and fill in real values.
# Sign up: https://www.alchemy.com (free tier covers AAVE + SKY mainnet reads at our cadence)
ALCHEMY_API_KEY=

# Optional fallback — Etherscan free tier (5 calls/sec, 100k calls/day)
# https://etherscan.io/apis
ETHERSCAN_API_KEY=
