// Holder Multiple landing — same cohort heat table as the root home, just
// served at the metric-themed URL so the nav reads cleanly. When we want
// the HM landing to diverge from `/`, this is where to do it.
import HomePage from "../page";

export default function HolderMultiplePage() {
  return <HomePage />;
}
